---
- name: Install baseline development packages
  ansible.builtin.apt:
    name: "{{ development_workstation_baseline_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600

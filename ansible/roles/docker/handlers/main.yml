---
- name: Restart docker
  ansible.builtin.systemd_service:
    name: docker
    state: restarted
    daemon_reload: true

- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 0

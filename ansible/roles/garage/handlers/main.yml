---
- name: Restart garage
  ansible.builtin.systemd_service:
    name: garage
    state: restarted
    daemon_reload: true
  when:
    - garage_config_stat.stat.exists | default(false)
    - not ansible_check_mode

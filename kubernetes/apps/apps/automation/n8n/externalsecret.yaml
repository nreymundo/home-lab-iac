apiVersion: k8s.bitwarden.com/v1
kind: BitwardenSecret
metadata:
  name: n8n-db-secrets
  namespace: n8n
spec:
  organizationId: "${BW_ORG_ID}"
  secretName: n8n-db-secrets
  authToken:
    secretName: bw-auth-token
    secretKey: token
  map:
    - bwSecretId: "737c521f-e53d-49ac-9865-b3d5010bf56e"
      secretKeyName: password
---
apiVersion: k8s.bitwarden.com/v1
kind: BitwardenSecret
metadata:
  name: n8n-secrets
  namespace: n8n
spec:
  organizationId: "${BW_ORG_ID}"
  secretName: n8n-secrets
  authToken:
    secretName: bw-auth-token
    secretKey: token
  map:
    - bwSecretId: "f202ae82-73f4-4c75-ba1c-b3d5010c1333"
      secretKeyName: N8N_ENCRYPTION_KEY

apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: audiobookshelf
  namespace: media
spec:
  chart:
    spec:
      chart: app-template
  values:
    controllers:
      main:
        containers:
          main:
            image:
              # renovate: datasource=docker depName=advplyr/audiobookshelf registryUrl=https://ghcr.io
              repository: ghcr.io/advplyr/audiobookshelf
              tag: 2.32.1
            env:
              TZ: Europe/Berlin
              CONFIG_PATH: /config
              METADATA_PATH: /metadata
              BACKUP_PATH: /metadata/backups
            ports:
              - name: http
                containerPort: 80
            resources:
              requests:
                cpu: 100m
                memory: 1Gi
              limits:
                cpu: 500m
                memory: 1Gi
    service:
      main:
        controller: main
        ports:
          http:
            port: 80
    ingress:
      main:
        annotations:
          traefik.ingress.kubernetes.io/router.middlewares: traefik-default-chain@kubernetescrd
          external-dns.alpha.kubernetes.io/hostname: audiobooks.lan.${CLUSTER_DOMAIN}
          gethomepage.dev/name: Audiobookshelf
          gethomepage.dev/description: Audiobook server
          gethomepage.dev/group: Media
          gethomepage.dev/icon: audiobookshelf.png
          gethomepage.dev/enabled: "true"
          # gethomepage.dev/widget.type: "audiobookshelf"
          # gethomepage.dev/widget.url: "http://audiobookshelf.media.svc.cluster.local"
          # gethomepage.dev/widget.key: '{{"{{HOMEPAGE_VAR_AUDIOBOOKSHELF_KEY}}"}}'
        hosts:
          - host: audiobooks.lan.${CLUSTER_DOMAIN}
            paths:
              - path: /
                service:
                  identifier: main
                  port: http
    persistence:
      data:
        enabled: true
        type: persistentVolumeClaim
        accessMode: ReadWriteOnce
        size: 1Gi
        storageClass: longhorn-r2
        globalMounts:
          - path: /config
            subPath: config
          - path: /metadata
            subPath: metadata
      media:
        type: nfs
        server: ${UNRAID_IP}
        path: /mnt/user/media
        globalMounts:
          - path: /mnt/media

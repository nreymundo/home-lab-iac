apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: garage-webui
  namespace: storage
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 3.2.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    controllers:
      garage-webui:
        containers:
          app:
            image:
              repository: khairul169/garage-webui
              tag: latest
            env:
              API_BASE_URL: "http://unraid.local:3903"
              S3_ENDPOINT_URL: "http://unraid.local:3900"
              API_ADMIN_KEY:
                valueFrom:
                  secretKeyRef:
                    name: garage-secret
                    key: API_ADMIN_KEY
    service:
      app:
        controller: garage-webui
        ports:
          http:
            port: 3909
    ingress:
      app:
        enabled: true
        className: traefik
        annotations:
          traefik.ingress.kubernetes.io/router.middlewares: traefik-gatekeeper-auth-chain@kubernetescrd
        hosts:
          - host: garage.lan.${CLUSTER_DOMAIN}
            paths:
              - path: /
                service:
                  identifier: app
                  port: http

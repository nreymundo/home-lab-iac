---
apiVersion: v1
kind: Service
metadata:
  name: syncthing
  namespace: external-proxy
spec:
  ports:
    - name: http
      port: 8384
      targetPort: 8384
---
apiVersion: v1
kind: Endpoints
metadata:
  name: syncthing
  namespace: external-proxy
subsets:
  - addresses:
      - ip: ${UNRAID_IP}
    ports:
      - name: http
        port: 8384
        protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: syncthing
  namespace: external-proxy
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: traefik-gatekeeper-auth-chain@kubernetescrd
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    external-dns.alpha.kubernetes.io/hostname: syncthing.lan.${CLUSTER_DOMAIN}
spec:
  ingressClassName: traefik
  rules:
    - host: syncthing.lan.${CLUSTER_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: syncthing
                port:
                  name: http

---
apiVersion: v1
kind: Service
metadata:
  name: chatterbox
  namespace: external-proxy
spec:
  ports:
    - name: http
      port: 8004
      targetPort: 8004
---
apiVersion: v1
kind: Endpoints
metadata:
  name: chatterbox
  namespace: external-proxy
subsets:
  - addresses:
      - ip: ${LLAMA_SWAP_IP}
    ports:
      - name: http
        port: 8004
        protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chatterbox
  namespace: external-proxy
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: traefik-gatekeeper-auth-chain@kubernetescrd
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    external-dns.alpha.kubernetes.io/hostname: chatterbox.lan.${CLUSTER_DOMAIN}
spec:
  ingressClassName: traefik
  rules:
    - host: chatterbox.lan.${CLUSTER_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: chatterbox
                port:
                  name: http

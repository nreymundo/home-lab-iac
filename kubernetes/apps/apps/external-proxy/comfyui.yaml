---
apiVersion: v1
kind: Service
metadata:
  name: comfyui
  namespace: external-proxy
spec:
  ports:
    - name: http
      port: 8188
      targetPort: 8188
---
apiVersion: v1
kind: Endpoints
metadata:
  name: comfyui
  namespace: external-proxy
subsets:
  - addresses:
      - ip: ${LLAMA_SWAP_IP}
    ports:
      - name: http
        port: 8188
        protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: comfyui
  namespace: external-proxy
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: traefik-gatekeeper-auth-chain@kubernetescrd
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    external-dns.alpha.kubernetes.io/hostname: comfyui.lan.${CLUSTER_DOMAIN}
spec:
  ingressClassName: traefik
  rules:
    - host: comfyui.lan.${CLUSTER_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: comfyui
                port:
                  name: http

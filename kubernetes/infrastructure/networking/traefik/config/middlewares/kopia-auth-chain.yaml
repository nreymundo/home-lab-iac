apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: kopia-auth-chain
  namespace: traefik
spec:
  chain:
    middlewares:
      - name: default-headers
      - name: authentik-auth-kopia
        namespace: authentik

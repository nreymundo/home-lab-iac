apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: opencode-streaming-headers
  namespace: traefik
spec:
  headers:
    customResponseHeaders:
      Cache-Control: "no-cache, no-transform"
      X-Accel-Buffering: "no"

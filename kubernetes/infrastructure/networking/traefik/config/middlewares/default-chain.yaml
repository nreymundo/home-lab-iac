apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: default-chain
  namespace: traefik
spec:
  chain:
    middlewares:
      - name: lan-allowlist
      - name: default-headers
      - name: crowdsec-bouncer
        namespace: crowdsec

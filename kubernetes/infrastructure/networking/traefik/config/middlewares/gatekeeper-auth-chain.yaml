apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: gatekeeper-auth-chain
  namespace: traefik
spec:
  chain:
    middlewares:
      - name: lan-allowlist
      - name: default-headers
      - name: authentik-auth
        namespace: authentik

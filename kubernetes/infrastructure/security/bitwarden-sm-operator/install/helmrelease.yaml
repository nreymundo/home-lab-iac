apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bwsmop
  namespace: flux-system
spec:
  releaseName: bitwarden
  interval: 30m
  targetNamespace: bitwarden-system
  install:
    createNamespace: false
  chart:
    spec:
      chart: sm-operator
      version: "0.*"
      sourceRef:
        kind: HelmRepository
        name: bitwarden
        namespace: flux-system
      interval: 1h
  values: {}

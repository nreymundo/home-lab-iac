apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: metallb-config
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/clusters/homelab/infrastructure/metallb/config
  prune: true
  dependsOn:
    - name: metallb-install
  sourceRef:
    kind: GitRepository
    name: flux-system

apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: node-feature-discovery-config
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/infrastructure/node-feature-discovery/config
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: node-feature-discovery

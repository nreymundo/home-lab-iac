apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gpu-plugin
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/infrastructure/gpu/intel-gpu-plugin
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: node-feature-discovery
